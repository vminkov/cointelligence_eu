CREATE TABLE T_USER (USERNAME VARCHAR(255) NOT NULL, AGE INTEGER, DEPARTMENT VARCHAR(255), FULLNAME VARCHAR(255), GENDER INTEGER, PASSWORDHASH VARCHAR(255), ROLE VARCHAR(255), ACCOUNT_ID VARCHAR(255), PRIMARY KEY (USERNAME))
CREATE TABLE T_TRANSACTION (ID VARCHAR(255) NOT NULL, AMOUNT BIGINT, CHECKEDBYMARKETMAKER TINYINT(1) default 0, ORDERTYPE VARCHAR(255), PRICEATTRADE BIGINT, STATE VARCHAR(255), ACCOUNT_ID VARCHAR(255), STATEMENT_ID VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE T_STATEMENT (ID VARCHAR(255) NOT NULL, CURRENTVALUE BIGINT, DESCRIPTION VARCHAR(255), TITLE VARCHAR(255), VOTESTARTED TINYINT(1) default 0, PRIMARY KEY (ID))
CREATE TABLE T_ACCOUNT (ID VARCHAR(255) NOT NULL, COINTELS BIGINT, PRIMARY KEY (ID))
CREATE TABLE Account_STATEMENTSINPOSSESSION (Account_ID VARCHAR(255), STATEMENTSINPOSSESSION BIGINT, STATEMENTSINPOSSESSION_KEY VARCHAR(255))
ALTER TABLE T_USER ADD CONSTRAINT FK_T_USER_ACCOUNT_ID FOREIGN KEY (ACCOUNT_ID) REFERENCES T_ACCOUNT (ID)
ALTER TABLE T_TRANSACTION ADD CONSTRAINT FK_T_TRANSACTION_ACCOUNT_ID FOREIGN KEY (ACCOUNT_ID) REFERENCES T_ACCOUNT (ID)
ALTER TABLE T_TRANSACTION ADD CONSTRAINT FK_T_TRANSACTION_STATEMENT_ID FOREIGN KEY (STATEMENT_ID) REFERENCES T_STATEMENT (ID)
ALTER TABLE Account_STATEMENTSINPOSSESSION ADD CONSTRAINT FK_Account_STATEMENTSINPOSSESSION_Account_ID FOREIGN KEY (Account_ID) REFERENCES T_ACCOUNT (ID)
